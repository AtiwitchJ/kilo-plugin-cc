{
  "name": "@agents-plugin-cc/kilo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use Kilo from Claude Code and Codex to review code or delegate tasks.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "test": "node --test tests/*.test.mjs"
  }
}